# Qwen3-TTS Requirements for DGX Spark / High VRAM GPUs
# Install with: pip install -r requirements-qwen3.txt

# Core ML
torch>=2.10.0
torchvision
torchaudio

# Qwen3-TTS
qwen-tts>=0.0.5
transformers>=4.57.0
accelerate>=1.12.0

# FlashAttention 2 (compile from source)
# pip install flash-attn --no-build-isolation
einops

# Audio processing
soundfile
scipy
pydub
librosa

# Utilities
tqdm
numpy
